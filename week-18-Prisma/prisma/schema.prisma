generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model User {
  id Int @id
  username String @unique
  password String
  age Int
}


model Todo {
  id Int @id
  title String
  description String
  done Boolean
  userId Int
}

